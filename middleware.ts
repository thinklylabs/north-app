// Middleware removed after migrating away from Clerk. If you need route protection,
// implement it in server components or API routes using Supabase session checks.
export default function middleware() {
  return;
}

export const config = {
  matcher: [],
};


